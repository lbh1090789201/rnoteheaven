sudo mysql -u root -proot rnoteheaven_production < db/users.sql
sudo mysql -u root -proot rnoteheaven_production < db/restaurants.sql
sudo mysql -u root -proot rnoteheaven_production < db/restaurant_albumns.sql
sudo mysql -u root -proot rnoteheaven_production < db/foodpackage_albumns.sql
sudo mysql -u root -proot rnoteheaven_production < db/user_infos.sql
sudo mysql -u root -proot  rnoteheaven_production < db/recharge_records.sql

sudo mysql -u root -proot  rnoteheaven_production < db/invitation_codes.sql
sudo mysql -u root -proot  rnoteheaven_production < db/consumer_records.sql
sudo mysql -u root -proot  rnoteheaven_production < db/validation_codes.sql
sudo mysql -u root -proot  rnoteheaven_production < db/foods.sql
