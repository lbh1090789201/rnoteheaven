module Webapp::FavoriteJobsHelper
  # user = FavoriteJob.where( :user => '1')
  # job = FavoriteJob.find_by_id(1)
  # user = FavoriteJob.find_by_id(1)
  # 获取用户的所有喜欢职位
  # def get_favorite_job(user_id)
  #    favorite_jobs = FavoriteJob.find_by_user_id(1)
  #
  # end

end
