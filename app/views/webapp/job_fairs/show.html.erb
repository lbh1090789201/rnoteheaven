<%= provide :title, @fair_name %>
<!-- <%# @fair_hospitals.each do |f| %>
<div class="hospital-list fair-list">
  <a href="/webapp/hospitals/<%# f[:hospital].id %>?fair_id=<%# f[:fair_id] %>">
    <div class="hospital-img" style="background: url(<%# asset_path(f[:banner]) %>) no-repeat;background-size: cover;"></div>
    <div class="hospital-bg"></div>
    <div class="hospital-note">
      <h2><%# f[:hospital].name %></h2>
      <p>人投递</p>
      <span><%# f[:apply_length] %></span>
    </div>
  </a>
</div>
<%# end %> -->

<div class="jobs">
  <div class="jobs-list" id="jobs_home_list">
    <% if @fair_jobs.length == 0 %>
      <p class="no-resurt">暂无入住机构</p>
    <% end %>
　　<% @fair_jobs.each do |j| %>
    <a href="/webapp/jobs/<%= j["id"] %>?fair_id=<%= params[:id] %>">
    <div class="job">
    <div class="title">
    <div class="left job-name"><%= j["name"] %></div>
    <div class="right right-range"><%= j["salary_range"] %>/月</div>
    <div style="clear:both"></div>
    </div>
    <div class="detail">
    <div class="left"><%= j["hospital_name"] %></div>
    <div class="right"><%= j["region"] %></div>
    <div style="clear:both"></div>
    </div>
    </div>
    </a>
    <% end %>
  </div>
</div>
