<!--
<style>
.btn {
    width: 100%;
    height: 50px;
    line-height: 30px;
    margin-top: 30px;
    margin-bottom: 30px;
}
</style>
<div class="container resume-show">
    <div class="wrap">
        <%= link_to "云康医聘", webapp_home_path, class: "btn btn-default" %>
        <%= link_to "demo", webapp_home_path, class: "btn btn-default" %>
        <%= link_to "双选会", webapp_job_fairs_path, class: "btn btn-info" %>
        <%= link_to("退出登陆",destroy_user_session_path, :method => :delete, class: "btn btn-danger" ) %>
    </div>
</div> -->
<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>demo</title>
 <%= javascript_include_tag "jquery-1.8.3.min" %>
  <script type="text/javascript">
  function onLoad(){
  	var u = navigator.userAgent;
  	var isAndroid = u.indexOf('Android') > -1 || u.indexOf('Adr') > -1; //android终端
      var mb = {
        "faction": "setBackCloseWeb",
        "parameter": "",
        "callback": ""
      }

      if(isAndroid){
      	window.js2MobInterface.postMessage(JSON.stringify(mb));
      }
  }
  </script>
</head>

<body>

<button onclick="onLoad()" style="margin-top: 30px;width: 70px;height: 60px;">刷新</button>
</body>
</html>
