<%= provide :title, "我的" %>
<div class="user-info">
  <div class="top-box" style="background:url(<%= image_path "preview_bg.png" %>)">
        <div class="avatar-box" style="background:url('<%= asset_path(@avatar) %>') no-repeat;" id="avatar_box">
            <!-- 已修改样式 bobo -->
        </div>
      <p><%= @hospital.name %></p>
      <p><%= @hospital.location %></p>
  </div>

  <ul>
    <li><a href="<%= edit_employer_user_path(@hospital.id) %>">医院信息设置</a></li>
    <li><a href="<%= employer_user_path(@hospital.id) %>">我购买的服务</a></li>
    <li>医才专场<span>参加中</span></li>
  </ul>
</div>



<%= render "/layouts/employer_footer" %>
